{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "mvn -B clean jetty:run",
            "group": "build"
        }
    ]
}
